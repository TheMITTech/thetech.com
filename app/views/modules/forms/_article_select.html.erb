<% issues = Issue.limit(5) %>
<%= grouped_collection_select :submodule, name, issues, :articles, :name, :id, :name, {include_blank: local_assigns[:include_blank]}, {multiple: local_assigns[:multiple]} %>
