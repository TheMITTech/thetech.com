<div class="page-header">
  <%= link_to authors_path, :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to edit_author_path(@author), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-pencil"></span>
    Edit
  <% end %>
  <h1><%= @author.name %></h1>
</div>

<dl class="dl-horizontal">
  <dt>Email:</dt>
  <dd><%= @author.email %></dd>

  <dt>Bio:</dt>
  <dd><%= @author.bio %></dd>
</dl>

<h2>Articles by this author</h2>
<table class="table" id="articles_table">
  <thead>
    <tr>
      <th>Section</th>
      <th>Article</th>
    </tr>
  </thead>
  <tbody>
    <% @published_versions.each do |article| %>
    <tr title="">
      <td><%= article.piece.section.name %></td>
      <td>
        <p class="headline"><%= link_to article.headline, article.piece.frontend_display_path %></p>
        <% if article.subhead? %>
        <p class="subhead"><%= article.subhead %></p>
        <% end %>
        <p class="byline">By <%= article.authors_line %>
          <% if article.bytitle? %>
          <span>— <%= article.bytitle %></span>
          <% end %>
        </p>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

